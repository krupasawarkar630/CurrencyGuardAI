<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <!-- Camera hardware declaration -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <application
        android:name=".CurrencyGuardApp"
        android:allowBackup="true"
        android:largeHeap="true"
        android:icon="@drawable/ic_launcher_foreground"
        android:label= "@string/app_name"
        android:roundIcon="@drawable/ic_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.CurrencyGuard">

        <!-- Main Dashboard & Bottom Nav host -->
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- CameraX Scanning Activity with Live Guide -->
        <activity
            android:name=".activity.ScanActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- AI Multi-stage Analysis Progress Activity -->
        <activity
            android:name=".activity.AnalysisActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Comprehensive Explainable Result Activity -->
        <activity
            android:name=".activity.ResultActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Ask CurrencyGuard AI Educational Chat Activity -->
        <activity
            android:name=".activity.ChatAssistantActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Scan History Detail & Passport View Activity -->
        <activity
            android:name=".activity.HistoryDetailActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Privacy Policy Activity -->
        <activity
            android:name=".activity.PrivacyPolicyActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Mandatory User Authentication Gate Activity -->
        <activity
            android:name=".activity.AuthActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Multi-Country Currency Converter Activity -->
        <activity
            android:name=".activity.CurrencyConverterActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- FileProvider for secure PDF / Image sharing -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
