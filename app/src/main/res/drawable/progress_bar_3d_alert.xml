<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Track Background (3D inset groove) -->
    <item android:id="@android:id/background">
        <shape android:shape="rectangle">
            <corners android:radius="8dp" />
            <solid android:color="#1A2438" />
            <stroke
                android:width="1dp"
                android:color="#243352" />
        </shape>
    </item>

    <!-- Progress Indicator with Alert Red/Orange gradient -->
    <item android:id="@android:id/progress">
        <clip>
            <shape android:shape="rectangle">
                <corners android:radius="8dp" />
                <gradient
                    android:startColor="#F59E0B"
                    android:centerColor="#EF4444"
                    android:endColor="#DC2626"
                    android:angle="0" />
            </shape>
        </clip>
    </item>
</layer-list>
